name: 🤖 Deploy Appwarden on Cloudflare
on: workflow_dispatch
# 1️⃣ Uncomment the following lines to deploy the middleware on every release
# deploy the middleware on every release
# on:
#   release:
#     types: [published]
# 1️⃣ or, uncomment the following lines to deploy the middleware on a push to a specific branch
# on:
#   push:
#     branches:
#       - main

env:
  # 2️⃣ Configure your deployment:
  #    - APPWARDEN_HOSTNAMES: JSON array of hostnames to deploy (e.g. '["example.com", "app.example.com"]')
  #    - CLOUDFLARE_ACCOUNT_ID: Your Cloudflare account ID (set as a variable in Github)
  #    - APPWARDEN_API_TOKEN & CLOUDFLARE_API_TOKEN: Add as secrets (https://docs.github.com/en/actions/security-for-github-actions/security-guides/using-secrets-in-github-actions)
  #     - Create APPWARDEN_API_TOKEN in the [Appwarden dashboard](https://use.appwarden.io/?to=settings/monitoring)
  #     - Create CLOUDFLARE_API_TOKEN in the [Cloudflare dashboard](https://dash.cloudflare.com/profile/api-tokens) using the "Edit Cloudflare Workers" template
  APPWARDEN_HOSTNAMES: '["example.com"]'
  CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
  APPWARDEN_API_TOKEN: ${{ secrets.APPWARDEN_API_TOKEN }}
  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

jobs:
  setup:
    name: Setup deployment matrix
    runs-on: ubuntu-latest
    outputs:
      hostnames: ${{ steps.set-matrix.outputs.hostnames }}
    steps:
      - id: set-matrix
        run: echo "hostnames=$APPWARDEN_HOSTNAMES" >> $GITHUB_OUTPUT

  deploy-appwarden:
    name: Deploy Appwarden to ${{ matrix.hostname }}
    needs: setup
    runs-on: ubuntu-latest
    strategy:
      matrix:
        hostname: ${{ fromJSON(needs.setup.outputs.hostnames) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4

      - name: Setup node
        uses: actions/setup-node@v4

      # 3️⃣ Uncomment the following lines if you're using `npm`
      # - name: Setup npm
      #   run: npm ci --ignore-scripts

      # 3️⃣ Uncomment the following lines if you're using `yarn`
      # - name: Install dependencies
      #   run: yarn install --ignore-scripts

      # 3️⃣ Uncomment the following lines if you're using `pnpm`
      # - name: Setup pnpm
      #   uses: pnpm/action-setup@v4
      #   with:
      #     run_install: |
      #       - args: [--ignore-scripts]

      # This builds the Appwarden middleware for Cloudflare
      - name: Build @appwarden/middleware
        uses: appwarden/build-cloudflare-action@v1
        with:
          hostname: ${{ matrix.hostname }}
          cloudflare-account-id: ${{ env.CLOUDFLARE_ACCOUNT_ID }}
          appwarden-api-token: ${{ env.APPWARDEN_API_TOKEN }}

      # This deploys the Appwarden middleware to Cloudflare
      - name: Deploy Cloudflare worker script
        uses: cloudflare/wrangler-action@v3.14.1
        env:
          APPWARDEN_API_TOKEN: ${{ env.APPWARDEN_API_TOKEN }}
        with:
          packageManager: npm # 4️⃣ add your package manager (e.g. npm, yarn, or pnpm)
          workingDirectory: .appwarden/generated-middleware
          environment: production
          accountId: ${{ env.CLOUDFLARE_ACCOUNT_ID }}
          apiToken: ${{ env.CLOUDFLARE_API_TOKEN }}
          secrets: |
            APPWARDEN_API_TOKEN
